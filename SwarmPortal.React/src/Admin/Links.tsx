import React from 'react';
import { ApiClient } from '../services/apiClient';

class AdminLinks extends React.Component<{client: ApiClient}, { }> {
    render(): React.ReactNode {
        return (
            <div></div>
        );
    }
}

export default AdminLinks;